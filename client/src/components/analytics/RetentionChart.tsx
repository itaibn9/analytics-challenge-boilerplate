import React, { useEffect, useState } from 'react';
import { Props } from "../../containers/DashBoard";

const RetentionChart: React.FC<Props> = ({weekData}) => {
    return (
        <div>
            HeY
        </div>
    )
}

export default RetentionChart
